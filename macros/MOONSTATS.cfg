[gcode_macro GET_DIR_STATS]
gcode:
    {% set dir = call_method("server.files.get_directory", path="~/klipper_config") %}
    RESPOND MSG={dir}

[gcode_macro GET_SYS_INFO]
gcode:
    {% set info = call_method("machine.system_info") %}
    RESPOND MSG={info}